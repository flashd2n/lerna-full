{
  "name": "root",
  "private": true,
  "scripts": {
    "test": "lerna run test",
    "release": "lerna publish --force-publish",
    "release-next": "lerna publish --force-publish --preid next",
    "bootstrap": "lerna bootstrap"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "lerna": "^4.0.0"
  }
}